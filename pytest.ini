# pytest.ini

[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py

markers =
    slow: marks tests as slow like a long Black Clover training arc


addopts = --cov --cov-report=html